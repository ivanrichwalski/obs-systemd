#
# Bash format script for common settings used
# to control options for the automated stream
#

# Location point to calculate sunrise and sunset
LATITUDE="37.82702N"
LONGITUDE="122.42295W"

# Offset for sunrise time
# Negative before sunrise, positive after sunrise
SUNRISE_OFFSET=-15

# Offset for sunset time
# Positive before sunset, negative after sunset
SUNSET_OFFSET=0
